{
  "name": "dead-rising",
  "private": true,
"scripts": {
  "test": "pnpm run test:jest && pnpm run test:vitest",
  "test:jest": "cross-env NODE_ENV=test jest \"tests/backend/**/*.test.js\"",
  "test:vitest": "vitest run"
},
  "devDependencies": {
    "@vue/test-utils": "^2.4.6",
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "jsdom": "^24.0.0",
    "mongodb-memory-server": "^10.1.4",
    "mongoose": "^8.16.1",
    "vitest": "^1.0.0"
  }
}